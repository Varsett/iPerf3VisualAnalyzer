# iPerf3 Visual Analyzer
A graphical tool for analyzing network test results generated by the iPerf3 utility using the UDP protocol

## Руководство пользователя/User manual  
### (English manual below)

---

## Что это такое

iiPerf3 Visual Analyzer — графический инструмент для анализа результатов тестирования сети утилитой iPerf3. Скрипт читает лог-файлы (.log, .txt), строит три графика (битрейт, джиттер, потери пакетов) и отображает аналитику по сессии. Поддерживает одновременное сравнение двух сессий на одних осях, автоматические скриншоты и запуск из bat-файлов.

![](https://github.com/Varsett/pictures/blob/31757d25c423c0e8a04f4c4b8548a9ca8c5c4951/iPerf3VA_v3.20main.jpg)
---

## Системные требования

- Windows 7 / 10 / 11
- PowerShell 5.1 или новее (встроен в Windows)
- .NET Framework 4.5+ (встроен в Windows 8+)

---

## Запуск

### Простой запуск (двойной клик или из командной строки)

Если рядом со скриптом лежат файлы `.log` или `.txt` — они загрузятся автоматически.  
Если файлов нет — откроется диалог выбора папки.

```
powershell -ExecutionPolicy Bypass -File iiPerf3_v3.ps1
```

### Через bat-файл (рекомендуется для автоматизации)

**Важно:** при передаче строковых параметров из bat-файла используйте `-Command`, а не `-File`.

```bat
@echo off
set "SCRIPT=%~dp0iiPerf3_v3.ps1"
set "LOGDIR=%~dp0"

powershell -WindowStyle Hidden -NoProfile -ExecutionPolicy Bypass ^
  -Command "& '%SCRIPT%' -Path '%LOGDIR%' -AView 'Direct' -BView 'Reverse' -Screenshot -Exit"
```

---

## Параметры командной строки

| Параметр | Описание |
|---|---|
| `-Path` | Путь к папке с логами или к конкретному файлу. Необязателен — если не указан, ищет файлы рядом со скриптом. |
| `-Label` | Название сессии или точки измерения. Отображается в блоке TEST INFO. |
| `-Interval` | Интервал отчётности iPerf3 (например `0.1s`). Отображается в блоке TEST INFO.  |
| `-Streams` | Количество параллельных потоков (параметр `-P` в iPerf3). Отображается в блоке TEST INFO.  |
| `-Duration` | Длительность теста (параметр `-t` в iPerf3). Отображается в блоке TEST INFO.  |
| `-TargetBitrate` | Запрошенная полоса пропускания (например `150M`). Отображается в блоке TEST INFO.  |
| `-WarnLoss` | Порог потерь в % для отображения предупреждения в заголовке окна. По умолчанию `1.0`. |
| `-AView` | Подстрока для поиска файла графика A. Регистронезависимо. При нескольких совпадениях выбирается самый свежий. |
| `-BView` | Подстрока для поиска файла графика B. Если не указан — второй график не загружается. |
| `-FileName` | Имя файла для сохранения скриншота PNG (без расширения). |
| `-Screenshot` | Автоматически сохранить скриншот при запуске. |
| `-Exit` | Закрыть окно сразу после сохранения скриншота. |

### Как работает -AView / -BView

Параметры ищут подстроку в именах файлов в указанной папке. Поиск нечувствителен к регистру. Если найдено несколько совпадений — выбирается файл с наиболее поздней датой изменения (`LastWriteTime`). Если дату изменения определить не удаётся, скрипт пробует распознать дату в имени файла по шаблону `YYYY-MM-DD_HH-MM-SS`.

**Пример файлов:**
```
WiFiTest-Direct-2026-05-13_01-24-51.txt   <- выбирается параметром -AView "Direct"
WiFiTest-Reverse-2026-05-13_01-24-51.txt  <- выбирается параметром -BView "Reverse"
```

---

## Интерфейс

### Панель управления (вверху)

| Элемент | Действие |
|---|---|
| Список A | Выбор файла для графика A (основной, сплошные линии) |
| Кнопка A | Скрыть / показать график A |
| Список B | Выбор файла для графика B (сравнительный, пунктирные линии) |
| Кнопка B | Скрыть / показать график B |
| Summary | Сводная таблица всех загруженных файлов |
| Save PNG | Сохранить скриншот всего окна |
| CSV | Экспортировать данные сессии A в CSV-файл |
| − / + | Уменьшить / увеличить масштаб по оси времени |
| Legend | Пояснение цветов и линий графиков |
| Help | Справка |
| Dark / Light | Переключение тёмной и светлой темы |

### Работа с графиками

| Действие мышью | Результат |
|---|---|
| Левый клик | Установить золотой маркер (точность до долей секунды) |
| Правый клик | Сбросить зум и маркер |
| Зажать левую кнопку и провести | Box-zoom: приблизить выделенный интервал |
| Колесо мыши (при зуме) | Прокрутка по оси времени влево / вправо |
| Ctrl + колесо мыши | Плавное приближение / отдаление |

Все три графика синхронизированы: зум и маркер применяются ко всем сразу.

### Tooltip при наведении

При наведении курсора на область графика всплывает подсказка с точными значениями Bitrate, Jitter и Loss в текущей точке. При загруженном графике B отображаются значения обоих датасетов.

---

## Графики

### Bitrate (Мбит/с)
Пропускная способность канала в каждый момент времени. Резкие падения указывают на перегрузку сети или помехи.

На графике отображаются пороговые линии:
- **MAX** — максимальный зафиксированный битрейт в сессии
- **90% (зелёная)** — отличное качество
- **75% (жёлтая)** — предупреждение
- **50% (красная)** — критическое состояние

### Jitter (мс)
Нестабильность задержки между пакетами. Критичен для голосовых звонков, видеоконференций и онлайн-игр.

Пороги:
- < 0.2 мс — отлично
- 0.2–0.8 мс — допустимо
- 0.8–1.2 мс — плохо
- > 1.2 мс — критично

### Loss (%)
Процент потерянных пакетов. Даже 1% потерь заметен на практике.

Порог 1% выделен на графике пунктирной линией.

---

## Сравнительный режим (A vs B)

Загрузите папку с несколькими файлами. Выберите разные файлы в списках A и B — графики отобразятся одновременно:

- **Сплошные линии** — файл A
- **Пунктирные линии** — файл B
- Заголовок окна показывает оба имени: `A: filename_a  |  B: filename_b`
- Tooltip отображает значения обоих датасетов в одной точке
- Кнопки **A** и **B** позволяют временно скрыть один из графиков

---

## Окно Summary

Открывается кнопкой **Summary**. Показывает таблицу с Min/Avg/Max по Bitrate, Jitter и Loss для каждого загруженного файла.

- Строки раскрашены по среднему уровню потерь: зелёный / жёлтый / красный
- **Одиночный клик** — выделить строку
- **Двойной клик** — загрузить файл как график B и закрыть окно

Ширина окна автоматически подстраивается под содержимое таблицы.

---

## Блок VERDICT (правая колонка, снизу)

Показывает итоговую оценку качества сессии по средним значениям.

### Оценки Loss
| Оценка | Условие |
|---|---|
| PERFECT | 0% потерь |
| GOOD | до 0.1% |
| MODERATE | 0.1–1.0% |
| HIGH | 1.0–3.0% |
| CRITICAL | выше 3.0% |

### Оценки Jitter
| Оценка | Условие |
|---|---|
| EXCELLENT | < 0.2 мс |
| GOOD | 0.2–0.8 мс |
| POOR | 0.8–1.2 мс |
| CRITICAL | > 1.2 мс |

### Оценки Bitrate
Сравнивается минимальный битрейт с максимальным в рамках сессии:

| Оценка | Условие |
|---|---|
| EXCELLENT | Min >= 90% от Max |
| GOOD | Min >= 75% от Max |
| POOR | Min >= 50% от Max |
| CRITICAL | Min < 50% от Max |

---

## Экспорт данных

### PNG скриншот
Кнопка **Save PNG** сохраняет скриншот всего окна рядом с логами (или в папке, указанной в `-Path`). Имя файла — имя выбранного лога или значение параметра `-FileName`.

### CSV
Кнопка **CSV** экспортирует данные текущей сессии A в текстовый файл с разделителями (Time, Bitrate_Mbps, Jitter_ms, Loss_pct) в кодировке UTF-8.

---

## Пример автоматического сценария

Задача: каждую ночь запускать iPerf3-тест, сохранять логи и автоматически строить сравнительный скриншот (прямой и обратный направления).

**Структура файлов:**
```
C:\WiFiTest\
  iiPerf3_v3.ps1
  run_test.bat
  WiFiTest-Direct-2026-05-14_02-00-00.txt
  WiFiTest-Reverse-2026-05-14_02-00-05.txt
```

**run_test.bat:**
```bat
@echo off
set "SCRIPT=%~dp0iiPerf3_v3.ps1"
set "LOGDIR=%~dp0"
set "DT=%date:~6,4%-%date:~3,2%-%date:~0,2%"

powershell -WindowStyle Hidden -NoProfile -ExecutionPolicy Bypass ^
  -Command "& '%SCRIPT%' -Path '%LOGDIR%' ^
    -Label 'Office WiFi' ^
    -AView 'Direct' -BView 'Reverse' ^
    -FileName 'Report-%DT%' ^
    -Screenshot -Exit"
```

Результат: файл `Report-2026-05-14.png` с обоими графиками в папке логов.

---

## Возможные проблемы

**Параметры не передаются из bat-файла**  
Используйте `-Command "& 'путь' -параметры"` вместо `-File`. При использовании `-File` кавычки и спецсимволы в аргументах обрабатываются иначе и могут потеряться.

**График не строится, "No valid UDP data found"**  
Файл не содержит строк с данными UDP (джиттер/потери). Убедитесь что iPerf3 запущен в режиме UDP (`-u`), а в логе присутствуют строки вида:
```
[  5]   0.00-1.00  sec  18.9 MBytes  159 Mbits/sec  0.123 ms  0/2381 (0%)
```

**Скрипт не запускается (политика выполнения)**  
Добавьте `-ExecutionPolicy Bypass` в строку запуска PowerShell.

---

*iPerf3 Visual Analyzer v3.2 — (c) 2026 Varset & Gemini Dev | rewrite by Claude*
